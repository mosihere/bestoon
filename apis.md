/submit/expense/
    POST, return a json
    inpit: date (optional), text, amount, user
    output: status:ok

/submit/income/
    POST, return a json
    inpit: date (optional), text, amount, user
    output: status:ok